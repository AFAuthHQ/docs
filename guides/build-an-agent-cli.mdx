---
title: "Build an agent (CLI)"
description: "Use the afauth CLI as your agent runtime — single static binary, no SDK required."
---

<Note>Stub — `afauth init`, `afauth discover`, `afauth signup`, then `afauth call` for ongoing requests.</Note>
