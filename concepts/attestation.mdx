---
title: "Attestation"
description: "Optional §10 mechanism for services to require attested agents."
---

<Note>Stub — explain when attestation is needed, how `Attestor` works (`HmacAttestor`, `JwksAttestor`, `MultiAttestor`), and the `attested_only` flag in §9.2.</Note>
