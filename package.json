{
  "name": "afauth-docs",
  "version": "0.1.0",
  "private": true,
  "description": "Documentation for the AFAuth protocol — docs.afauth.org",
  "scripts": {
    "dev": "mint dev",
    "validate": "mint validate",
    "broken-links": "mint broken-links",
    "sync:spec": "tsx scripts/sync-spec.ts",
    "sync:typescript-sdk": "tsx scripts/sync-typescript-sdk.ts",
    "sync:cli": "tsx scripts/sync-cli.ts"
  },
  "devDependencies": {
    "mintlify": "^4.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
