---
title: "afauth CLI"
description: "Reference command-line agent runtime — single static binary, no SDK required."
---

<Note>Stub — what the CLI is, when to reach for it instead of the SDK.</Note>

```bash
afauth init
afauth signup https://api.example.com
afauth call https://api.example.com/afauth/v1/accounts/me
```

See [Install](/cli/install) to get started.
