name: Sync from AFAuthHQ/typescript-sdk

# Triggered by AFAuthHQ/typescript-sdk on release-tag.
# Payload (client_payload): { tag: string, run_id: string }

on:
  repository_dispatch:
    types: [typescript-sdk-released]
  workflow_dispatch:
    inputs:
      run_id:
        description: "GitHub Actions run_id in AFAuthHQ/typescript-sdk to pull the typedoc artifact from"
        required: true

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      # TODO: wire up once AFAuthHQ/typescript-sdk publishes a typedoc artifact.
      # Suggested approach:
      #   - actions/download-artifact@v4 with run-id and github-token
      #   - Run pnpm sync:typescript-sdk to transform/place files under sdk/typescript/*/reference/
      #   - peter-evans/create-pull-request@v8 with label `automerge: typescript-sdk-sync`
      #   - gh pr merge --auto --squash
      - name: Placeholder
        run: |
          echo "ℹ sync-typescript-sdk is a stub. Wire up the typedoc artifact pipeline in AFAuthHQ/typescript-sdk first."
